{
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "export PATH=\"/usr/bin:$PATH\" && node --version && pnpm install",
  "start": "export PATH=\"/usr/bin:$PATH\" && sudo service docker start",
  "terminals": [
    {
      "name": "postgres",
      "command": "export PATH=\"/usr/bin:$PATH\" && docker compose up postgres",
      "description": "PostgreSQL 17 for integration tests on 127.0.0.1:5432"
    }
  ]
}
